const emailLogger = (message) => {
    console.log(`
    ================================
    EMAIL LOG
    -------------------------------
    ${message}
    ================================
    `);
};

module.exports = emailLogger;
